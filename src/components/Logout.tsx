import LogoutIcon from "@mui/icons-material/Logout";
import { IconButton } from "@mui/joy";

function Logout() {
  return (
    <IconButton>
      <LogoutIcon />
    </IconButton>
  );
}
export default Logout;
