export const WelcomeRoutes = {
  PROFILE: "/profile",
} as const;

export const AppRoutes = {
  HOME: "/",
  CHARACTERS: "/characters",
  SPELLS: "/spells",
} as const;

export const PublicRoutes = {
  LOGIN: "/login",
  SIGNUP: "/signup",
} as const;
