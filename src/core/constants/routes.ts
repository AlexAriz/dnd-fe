export const HiddenPaths = {
  ROOT: "/",
  PROFILE: "/profile",
} as const;

export const Modules = {
  CHARACTERS: "/characters",
  SPELLS: "/spells",
  FEATS: "/",
} as const;

export const PublicRoutes = {
  LOGIN: "/login",
  SIGNUP: "/signup",
} as const;
